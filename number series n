#include <stdio.h>
#include <conio.h>

int main()
{
    int i=1,s=1,n;

    printf("Enter nth term ");
    scanf("%d",&n);

    while(i<=n)
    {
        printf("%d ",i*s);
        s=s*-1;		// changing the sign
        i=i+1;
    }
    return 0;
}
